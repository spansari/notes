## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://localhost/app_db
spring.datasource.jdbc-url = jdbc:mysql://localhost/app_db
#spring.datasource.username = sa
#spring.datasource.password = password
spring.datasource.database = app_db

## Hibernate Properties

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

bootstrap.address = localhost:9092

server.servlet.context-path=/notes

config-service-url = http://localhost:8080/json-config/api/

hello-service-url = http://localhost:8081/hello

#logbook
logbook.write.level=info
logbook.write.category=worklog-trace

#actuator
management.endpoint.health.show-details=always

# Vault
spring.cloud.vault.host=localhost
spring.cloud.vault.port=8200
spring.cloud.vault.scheme=http
spring.cloud.vault.authentication=token
spring.cloud.vault.token=5msqeYVtdGIYy59qBWxnApcB
spring.cloud.vault.kv.enabled=true
spring.cloud.vault.generic.enabled=true
spring.cloud.vault.generic.backend=backstage
